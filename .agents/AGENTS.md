# Project Rules

- **Live Deployment**: Whenever code changes are made to the Flutter application, you MUST automatically run `docker compose up -d --build fireraccoon-web` to rebuild and restart the web container so the user can see the resulting changes live.
