name: fireracoon_engine
description: >-
  Pure Dart core for FireRacoon — Firefly III client, projection engine, and
  dashboard analytics. Shared by the Flutter app and the MCP server.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  crypto: ^3.0.7
  http: ^1.6.0
  intl: ^0.20.2
  logging: ^1.3.0

dev_dependencies:
  test: ^1.31.0
  lints: ^6.0.0
  coverage: ^1.15.0
